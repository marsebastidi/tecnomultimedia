

class Bulto{
  
  1)//propiedades_variables
  //ubicacion en x, en y, tamaño, color, dirección, etc
  
  
  2)//se llama al -constructor- (a.k.a, el setup de la clase)
  //se inicializa el mismo con las variabes declaradas previamente
  
  Bulto(/*posx,posy,t*/){
   //x= ;
   //y= ;
   //t= _t;
   
  
//definimos en acciones en infinitivo las funciones o métodos que corresponden
//al/ los objetos

void actualizo(){//actualizamos los valores de las variables de la clase
  //posx= velx;
  //posy= y+vel;
  
  void dibujo(){}
}

  void avanzarRetroceder(){
  //variables de dirección, velocidad
}

  void accionar(){
    //uso de un if y de alguna funcion o boolean que determine cuando haya o colisión
    //o dos elementos coincidan en distancia+tamaño, para dar las condiciones de accion
    //con otros objetos
}

  void recepción(){
    
   //respuestas a las interacciones del objeto 1 con el objeto 2
   //if estado == x; rta x; if estado == y; rta y;
    
    
  }

void keypressed(){
  //determinar las teclas para permitir la interactividad del usuario con el programar
}
